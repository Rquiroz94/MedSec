// Archivo principal generado desde ChatGPT para MaiMed

export default function Page() {
  return <div>MaiMed app funcionando</div>;
}/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
